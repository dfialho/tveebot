ext {
    romeVersion = '1.0'
}

dependencies {
    implementation "rome:rome:$romeVersion"
}


// Allows test code to be shared with other projects
configurations {
    testOutput
}

dependencies {
    testOutput sourceSets.test.output
}