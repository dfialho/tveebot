ktor {
  deployment {
    port = 8080
  }

  application {
    modules = [dfialho.tveebot.app.AppKt.app]
  }
}

tveebot {
  checkPeriod = 30 // seconds
  downloadingDirectory = ${HOME}/.tveebot/downloads,
  libraryDirectory = ${HOME}/.tveebot/library
  databasePath = ${HOME}/.tveebot/repository
}
